# 昨日之我功能审查

审查起点：`9f9aa7f5c9a1272f792630f065e1c7326d5a6a7a`。对照 master `bfb280a0bb57c5ef1a006b4b7430037575bff666`，覆盖 D 改版、其他模型筛选及现有搜索调用链。用户已授权审查后合入。

## 发现并修复

1. **P1：长 Unicode 原文翻页漏字。** SQLite 的 `length/substr` 按 Unicode 码点定位，预览接口却用 JavaScript UTF-16 长度推进游标。含 emoji 的长原文可能跳过内容或提前结束。现在游标、截断判断使用同一套码点计数，回归将多页重组后逐字比较。
2. **P2：产物已打开却被搜索遮住。** 搜索对话层覆盖主预览面板，文件打开成功后旧 UI 没有退出搜索。现在成功打开 Hub 预览或文件管理后才关闭搜索；失败和过期异步操作不会关闭当前阅读。Markdown 文件链接走相同路径。
3. **P2：失效原文显示空白。** 后端 `state: stale` 带 `session` 元信息和 `error`，UI 只检查 session 是否存在，漏掉了错误。现在显示后端具体错误，仍可返回结果重新搜索。
4. **P2：项目库晚返回会关闭阅读抽屉。** 项目请求成功/失败均调用重新搜索，而 D 的重新搜索会关闭 reader。现在阅读中仅显示有更新的提示，待返回结果后刷新，不打断当前阅读。

上述问题均先构造复现，再修复；单测复现记录保存在 `output/yesterday-review-repro.log`，产物遮挡在本次真实 CDP 审查中复现。新增 E2E 为 `tests/e2e-yesterday-review-cdp.js`。

全量门禁补充：恢复「继续检索中」进度文案，保持标题和全文检索共用的既有提示契约。Windows 门禁环境需将 Git 的 `bin` 目录加入 PATH，使开发流程测试能够执行真实 shell hooks；不修改或跳过这些测试。

## 覆盖矩阵

| 功能 | 证据 |
|---|---|
| 输入、中文/英文、标题/提问/回答、来源、项目、时间 | 搜索单测 + global-session-search CDP |
| 其他模型 × 归档 × Agent、空结果、来源计数 | session-search-other CDP |
| 结果翻页 50→60、无重复、标题升降序、置顶 | 新增审查 CDP |
| 概览、命中位置、原始对话前后分页 | 新增审查 CDP |
| 长原文展开、emoji 跨页完整性 | 新增审查 CDP + 码点回归单测 |
| 复制引用、产物发现和实际打开 | 新增审查 CDP；剪贴板写入使用 recorder，不覆盖用户剪贴板 |
| 文件被移动后仍读索引、显式告知原文件缺失 | 新增审查 CDP |
| Markdown、代码、公式、只读卡片、恶意 HTML 不执行 | global-session-search CDP + 单测 |
| 方向键、Enter、Esc、偏好、重新打开、四档尺寸 | session-search-other CDP |
| 打开群聊、继续 Claude 原会话并精确高亮 | global-session-search CDP；原生模型使用协议夹具 |
| 过期请求、项目读取失败、预览失效、文件打开失败 | UI 单测 |

专项搜索单测修复后 141/141 通过。全部 E2E 在独立数据/home/CDP 的真实 Electron 实例执行，不操作生产 Hub，不使用真实模型账号。合并前还须在隔离完整副本运行项目合并 dry-run，全量结果及最终合并 SHA 记入交付报告。

## 边界

- “其他”表示非 Codex/Claude 的模型会话，群聊单列。无完整解析器的来源只有标题/已有输出摘要可搜索，不承诺无损原生归档。
- 历史文件已丢失时可阅读已索引内容；不能据此保证原文件和所有附件仍存在。
- 窄窗口检查约束搜索界面，宿主背景原有的 375px 横向溢出不在本次改动范围。
- 审查与合并不会重启当前生产窗口；更新在用户下次正常启动 Hub 时加载。
