# AI Hub 交互统筹与验收

用户目标：从整体设计视角清理历次开发形成的重复 UI 与功能，交付经过验证并合入主干的版本。头像指定收件人是已确认的交互约束。

## 统一职责

| 界面 | 唯一职责 | 应移除的歧义 |
| --- | --- | --- |
| 输入框头像 | 决定下一条话发给谁 | 不用工作流角色、正文 @ 或回复状态暗改收件人 |
| 群成员栏 | 管理成员、打开 CLI、查看个体状态 | 成员总数不再冒充选中收件人数 |
| 工作流进度 | 说明当前步骤、交付与可执行的下一步 | 不把回答结束数当成交付进度；暂停、接续不同时堆叠 |
| 聊天时间线 | 用户要求、AI 回复及必要事件 | 自动派工不署名为“我”；文件协议默认收起但原文可查 |
| 群聊工具 | 搜索历史、查看详细轮次 | 不承担另一个流程控制中心 |
| 工作目录与任务文件 | 分别查看项目文件、当前任务交付 | 标明范围，避免两个同名文件入口 |
| 首页和功能导航 | 进入对应工作空间 | 活动群聊不得与欢迎页争夺主要阅读空间 |
| 账号、能力、记忆等全局页 | 管理全局资源 | 不重复创建群聊专属设置与在途任务状态 |

## 交付顺序与验收依据

1. 头像投递和阶段联动：已有候选 558a24a；真实 CLI 接收、持久回执和阶段不变均有证据。全量测试尚未放行。
2. 工作流控制与聊天可读性：默认展示状态、一个主要操作及交付文件；高级恢复/结束操作按需展开；原文不删。加载和失败都有明确重试入口，切群期间迟到响应不操作新群。
3. 群聊布局和导航：检查普通群、文件工作流、旧开发群、成员双屏、工具展开、小窗口与空选择；核对入口作用和重复状态。
4. 全局入口审计：检查首页、工作台、备忘录、记忆、工具、账号的入口归属，以及本轮其他工作位正在改的初心内导航。保留不同职责的入口，不按视觉相似就删除功能。
5. 集成：独立审查；真实隔离 Hub 与 GUI 证据；全量项目验证；核实其他在途合并结束后走项目合并入口，升版本、推送、核验主干。

完成标准是上述范围在最终主干上的证据，不以候选提交、单元测试或设计文档代替。当前仍在实施；不改生产实例，不合并或清理他人的未提交文件。

## 本轮界面审计与范围收口

- **群聊**：输入框头像承担当条消息路由，工作流切步时给出下一步默认选中；用户手动选择在当前阶段保留。群成员按钮只显示总人数。派工卡显示目标与步骤，完整协议可展开，署名为“工作流”。工作流条只展示文件交付进度；暂停、恢复及高级操作按状态出现。
- **文件入口**：顶部工作目录入口打开项目文件；“交付文件”打开当前运行的任务目录。两者范围不同，保留入口并采用不同名称，不合并成容易打开错目录的按钮。
- **全局导航与首页**：既有陶瓷导航和首页工作台已有明确入口，真实隔离 Hub 的 18 项导航检查、19 项首页检查通过；覆盖主题、尺寸、125% 缩放及账号、工具、记忆和会话创建。未另加一套导航。
- **开发工作台**：18 项真实 GUI / 文件 / IPC 检查通过；保持只读项目聚合，修改任务继续进入群聊。文件更新、修订倒退保护、历史与讨论分区、五种主题、窄宽度均有证据，不把“报告完成”说成“已合并”。
- **学习**：仪表盘负责课程、材料和教练入口，交谈复用普通会话。旧 E2E 仍断言 9 月 8 日 d76c610 删除的收纳芯片，现改为验证两个教练身份、重复打开复用、侧栏可达和主区互斥，并补齐隔离 Codex 账号配置。33 项通过；没有发送模型 prompt，不宣称学习质量通过。
- **初心投研**：另一个工作树的内嵌导航候选 `cf6cfe0` 尚未合入；本轮没有把它作为已交付内容。其跨部署握手与旧版回退需要独立验证，不把视觉去重凌驾于功能可达性。

生产代码候选 `1b0b837bbd714634a4f94c4ce35affd6fdbd85ba` 已通过只读独立复核，未发现剩余 P0/P1/P2。群聊 GUI 13 项使用原生协议夹具；此前头像路由另有真实 Claude / Codex PTY 插话证据。夹具交付、真实 CLI 接收、全量单测和正式合并是分开的证据，不能互相替代。完整回归与合并结果以交付报告为准。
