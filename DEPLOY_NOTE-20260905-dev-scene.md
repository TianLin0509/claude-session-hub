# 2026-09-05 · v1.6.62 · 开发场景：一个通用工作流 + 主干闸门 + 开发看板

## 一句话

「开发」场景在 Hub 里本来就有（`MEETING_MODES` 白名单、建群单选按钮、角色帽子都在），
但**是个空壳** —— 选了它什么也不会发生。这一轮把它填实，并补上此前完全缺失的主干闸门。

## 为什么需要闸门（不是锦上添花）

`core.hooksPath` 一直指着 `.githooks`，但**那个目录从来没被提交过**，钩子从头到尾没工作过。
30 天 142 次提交、峰值 19 次/天，零闸门 —— 这就是「多 agent 互相覆盖」的直接来源。

防覆盖靠两件事，缺一不可：

| | 解决什么 | 谁管 |
|---|---|---|
| 各干各的 | 干活时互相覆盖 | `pre-commit`（主目录禁提交，逼你开 worktree） |
| 一次只落一个 | 落地时冲突、落了没验的东西 | `pre-push` + `scripts/merge_task.py` |

因为所有 worktree 共用同一个 `.git`，**一个 `pre-push` 钩子对全部 worktree 同时生效** ——
这正是本地闸门能成立、不必依赖 GitHub 分支保护的原因。

**`.gitattributes` 里 `.githooks/* text eol=lf` 那行不能少**：Windows 会把钩子转成 CRLF，
`#!/bin/sh\r` 变成坏解释器，钩子**静默失效** —— 而静默失效正是最危险的失败方式。

## 流程：所有项目走同一条

```
你在输入框打一句人话 → 回车
  ① 工作位：开自己的 worktree → 实现 → 自测 → 四行人话卡
  ② 合并位：读 diff → 亲自跑测试 → RESULT: PASS / FAIL
  ③ FAIL 自动回①（≤3 轮）；PASS 由合并位调 merge_task.py 合入
```

**不需要「开跑」按钮**：发送按钮本来就按 `serialWorkflow.loop.enabled` 三岔路分发
（`meeting-room.js` 的 `doSend`）。建群选「开发」时自动写入工作流（`_applyDefaultDevWorkflow`），
之后你只剩「打字 + 回车」。想随便问一句就关掉工作流开关，走原来那条普通群聊路径。

**SuperRAN 的内网评审不再是流程里的一个阶段**，而是两个独立任务之间的空隙：
任务 A 合并后产出 zip → 你拿去内网 → 意见存回来 → 任务 B「按意见改」。系统不需要知道内网这回事。

## 底座通用，项目差异下沉

预设 prompt 里**一个项目名、一条绝对路径都没有**，只写 `.agents/AUTHOR.md` 这类仓库内相对路径 ——
群聊工作目录就是项目根，所以对任何整理过的项目都成立。

项目差异全部读各自仓库的 `.agents/project.json`（主干名、测试命令、合并后动作、合同路径）。
**接新项目不用改 Hub**，跑一次 `project-prep` skill 即可。

`unit-dev-scene-contract.test.js` 守着这条：谁把绝对路径或项目名写回 Hub，测试立刻红。

## 开发看板

一个群聊 = 一个任务，所以**看板一行就是一个开发群聊**，数据全部推导自 Hub 自己的会议列表，
不扫仓库、不读 Agent 写的台账 —— Agent 会忘记更新，但循环状态是客观的。

推导优先、申报兜底：阶段/轮次/闲置全推导；只有「干了什么/什么风险」这类推导不出来的
才读 Agent 申报的四行。导航按钮从「RAN」改成「开发」。

## 实测查出的坑（都是会静默失效的那类）

| 问题 | 后果 |
|---|---|
| `loop-engine` 里硬编码超时（工作位 10 分钟 / 合并位 5 分钟） | 预设配的 30/25 分钟从没被读到；合并位要跑几百个测试，5 分钟必然被砍断 |
| 阶段推导用错字段名（`running`/`stepIndex` vs 真实的 `status`/`currentStep`） | 正在跑的任务在看板上显示「未开始」 |
| 通用评审 prompt 写「绝不修改任何代码」，与合同要求合并位执行合并冲突 | 合并位卡在「我没有权限」 |
| `MERGER.md` 让合并位合完后改用工作位的四行格式 | 切走了 `RESULT` 协议，引擎认不出 PASS，循环空转到轮次用尽 |
| `merge_task.py` 无互斥 | 两个合并位会同时 checkout 主工作区，互相踩（已加文件锁） |
| 合并脚本自己的测试没按 `unit-*` 命名 | 运行器收不到它，一直没被跑过 |

## 这一轮实测查出并修掉的（B2→B7 七次真实多 AI 验收）

按「会不会让你用起来撞上」排序：

| # | 问题 | 症状 | 状态 |
|---|---|---|---|
| 1 | 扫描器被单个文件焊死 | codex 答完了、Hub 一直转，循环挂死 | 已修（限时 + 自愈）v1.6.67 |
| 2 | 扫描心跳停了没人发现 | 同上，但不是焊死，是循环整个不跑了 | 已修（心跳重启）v1.6.72 |
| 3 | 裁决被提前结算的短文本截断 | 代码合对了，引擎却判失败、烧满三轮 | 已修（等文本不再增长）v1.6.63 |
| 4 | 工作位那一步同样会被提前结算 | 评审看到的是半成品分支 | 已修 v1.6.65 |
| 5 | 席位不可用被当成「没给裁决」 | 评审没额度时白烧两轮，还显示成「返工用尽」 | 已修（专属终态）v1.6.68 |
| 6 | 四种停止原因都显示成「已停止」 | 你分不清是自己停的、超时、卡住还是出错 | 已修 v1.6.64 |
| 7 | 人话四行只渲染了一行 | 验证证据 / 风险 / 报告链接全被丢掉 | 已修 v1.6.69 |
| 8 | 「工作区一脏就拒绝」锁死闸门 | 一份陈年半成品让所有合并都跑不了 | 已修（精确判据）v1.6.70 |
| 9 | 合并脚本被中途杀掉留下未验证的合并 | 主干停在「合了但没测」且无提示 | 已修（测过才提交）v1.6.71 |
| 10 | 源码里混进字面退格符 | 正则看着一样但永远不匹配，静默失效 | 已修 + 守门测试 |

**其中 1/2/3/9 是「不报错、只是悄悄不工作」那一类** —— 读十遍代码也未必发现，
只有真跑起来才暴露。这也是为什么验收必须真跑而不是只看单测。

## 还没验完的一段

**「合并位在真实群聊里调用合并脚本」这条链没有在一次完整循环内跑通过。**

已有的证据是分开的：
- 合并脚本本身：一次性沙箱压过 8 项（正常合入 / 测试红了回滚 / 并发抢锁 /
  脏工作区精确判据 / 回滚不抹别人的活 / 中途被杀主干不前进）
- 合并位的真实行为：B5 的 reflog 实证过它按合同做了 `--dry-run` → 验证 → 真合
  （额度恢复后 2 分钟内完成，只是那时循环早已放弃、没人记录）

差的是**在一个循环内**看到「评审给 PASS → 引擎记下 → 合并落地 → 状态收成 done」。
在这条补齐之前，**看板对开发任务成败的显示不要全信，以仓库实况（git log）为准**。

## 验证

- 347 个单测全过（新增约 60 条，覆盖裁决取文本 / 扫描自愈 / 席位不可用 / 看板渲染 / 合并闸门）
- `tests/dev-scene-cdp-e2e.js`：隔离 Hub 起真实 renderer，13 项全过
- `tests/unit-merge-task-sandbox.test.js`：一次性 git 仓库跑合并脚本真实路径 ——
  正常合入 / 测试红了自动回滚且工作区干净 / 脏工作区拒绝且不冲掉别人文件 / 并发第二个被拒
- 确定性压力（unit-dev-flow-stress，12 条，不依赖 AI 可反复跑）：
  8 个合并位真并发抢锁 / 连续合入 20 个任务 / 主干移动后仍能合 / 5 个 worktree 下钩子行为 /
  看板喂畸形与 300 行 / 冲突后完整回滚 / 中途点停止 / 脏工作区三种情形 / 脚本被杀主干不前进
- 端到端：真实群聊修好过两个真 bug（average、median）并合入主干，棘轮双向反证成立、已独立复核

## 相关

- 取代 `DEPLOY_NOTE-20260904-ran-workflow-presets.md`
- skill：`~/.claude/skills/project-prep/`（把仓库整理成可并行开发的形态，一个项目跑一次）
